# auxdeck-shell

Tauri v2 + React 渲染层（CLAUDE.md §3 / §9）：网格布局 + 多页切换 + 各 widget 卡片，
订阅 daemon 的 WebSocket 推送，自身不做任何采集。

构建必须经 tauri CLI（`npm run tauri build` / `npm run tauri dev`），原因见根 README「运行」一节。
